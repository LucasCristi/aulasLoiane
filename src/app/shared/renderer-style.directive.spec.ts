import { RendererStyleDirective } from './renderer-style.directive';

describe('RendererStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererStyleDirective();
    expect(directive).toBeTruthy();
  });
});
